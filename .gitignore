# Member data — contains personal details, never commit
*.csv
*.pdf

# Local backups and shortcuts
.env.bkp
*.lnk

# Python
__pycache__/
*.py[cod]

# Local environment config (not used by the code; see .env.example)
.env*
!.env.example
